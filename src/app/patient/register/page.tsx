import { PatientRegisterForm } from "@/features/auth/patient-register-form";

export const metadata = { title: "Patient Register — CaseTaker" };

export default function PatientRegisterPage() {
  return <PatientRegisterForm />;
}
