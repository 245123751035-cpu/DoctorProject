import { PatientLoginForm } from "@/features/auth/patient-login-form";

export const metadata = { title: "Patient Login — CaseTaker" };

export default function PatientLoginPage() {
  return <PatientLoginForm />;
}
